pre-commit:
  commands:
    format:
      run: ./gradlew ktlintFormat

pre-push:
  parallel: true
  commands:
    ktlint:
      run: ./gradlew ktlintCheck
    detekt:
      run: ./gradlew detekt
    test:
      run: ./gradlew :core:domain:test
